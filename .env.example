# Copy to .env and fill in real values. .env is gitignored — never commit it.

# Supabase new-format secret key (sb_secret_…)
# Dashboard → Project Settings → API Keys → Secret keys
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxxxxxxx

# Optional: Postgres direct connection string (used by dc_init-db.mjs)
# DATABASE_URL=postgresql://postgres:[DB_PASSWORD]@db.tixgzezefjjsyuzgdhcd.supabase.co:5432/postgres
